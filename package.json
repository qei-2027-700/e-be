{
  "name": "e-be",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "dev:web:clean": "rm -rf apps/web/.next && turbo dev --filter=web",
    "dev:docs": "turbo dev --filter=docs",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "seed:test": "node apps/web/scripts/seed-test-accounts.mjs",
    "seed:events": "node apps/web/scripts/seed-events.mjs"
  },
  "devDependencies": {
    "turbo": "^2.9.4"
  },
  "packageManager": "pnpm@9.7.1",
  "engines": {
    "node": ">=20.9.0"
  }
}
